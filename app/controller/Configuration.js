Ext.define('Xnfy.controller.Configuration', {
    extend: 'Ext.app.Controller',
    models: [
        'Configuration'
    ],
    stores: [
        'Configuration'
    ],
    views: [
        'Configuration'
    ],
    init: function(application) {
        this.control({
        });
    }

});
